QUARANTINE POLICY

This DLL is copied only for a future isolated inference worker.
The PyQt desktop process never imports or loads this file.
Its approved SHA-256 is recorded in data/import-manifest.json.
Do not enable it until the restricted worker and real-machine validation are complete.
